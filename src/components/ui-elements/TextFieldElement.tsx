import React from 'react';
import TextField from '@mui/material/TextField';

const TextFieldElement: React.FC<any> = (props) => {
    return <TextField {...props} />;
};

export default TextFieldElement;